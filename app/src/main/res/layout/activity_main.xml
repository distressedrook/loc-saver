<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/application_base_color"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="@string/application_name"
        android:id="@+id/home_screen_app_name"
        android:textColor="#ffffff"
        android:textSize="50sp"

        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="120dp" />

    <Button
        android:layout_width="250dp"
        android:layout_height="60dp"
        android:text="@string/home_screen_pick_location"
        android:id="@+id/show_map_button"
        android:background="@drawable/button_border"
        android:layout_marginTop="50dp"
        android:layout_below="@+id/home_screen_app_name"
        android:layout_centerHorizontal="true"
        android:textColor="#ffffff"
        android:stateListAnimator="@null"/>

    <Button
        android:layout_width="250dp"
        android:layout_height="60dp"
        android:text="@string/home_screen_saved_locations"
        android:id="@+id/saved_locations_button"
        android:background="@drawable/button_border"
        android:layout_marginTop="40dp"
        android:layout_below="@+id/show_map_button"
        android:textColor="#ffffff"
        android:layout_centerHorizontal="true"
        android:stateListAnimator="@null"/>

</RelativeLayout>
